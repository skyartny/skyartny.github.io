# Flex-Check Pro

Flex-Check Pro 是一个轻量级、支持离线的学生签到和花名册管理 Web 应用程序。它专为简化学生出勤追踪、课时余额管理以及家长通知流程而设计。

采用“本地优先”架构构建，所有数据通过 IndexedDB 实时存储在您的浏览器中。

## ✨ 主要功能

### 📋 签到系统 (Check-In System)
*   **快速搜索**: 支持按姓名即时查找学生。
*   **一键签到**: 点击签到后，系统会自动从学生余额中扣除 **1 课时**。
*   **视觉状态**:
    *   **Checked In (已签到)**: 当天已签到的学生会显示在列表底部，并带有对勾标记。
    *   **Classes Left (剩余课时)**: 显示剩余课时数 (例如: "5 Classes Left")。
    *   **余额不足提示**: 负余额会以红色高亮显示。

### 🛡️ 管理员仪表盘 (Admin Dashboard)
*   **安全访问**: 密码保护的管理区域，使用 SHA-256 哈希加密。
*   **简易设置**: 首次使用时设有简单的向导，用于创建管理员密码和安全问题。
*   **密码找回**: 可通过预设的 **Security Question** (安全问题) 重置密码。
*   **学生管理**:
    *   **查看/编辑**: 直接编辑学生详细信息（名字、姓氏、家长邮箱、电话）。
    *   **重名防止**: 自动检测重复姓名并建议替代名称（例如 "John Doe 2"）。
    *   **激活/停用状态**: 可切换学生状态，在不删除数据的情况下将其从签到列表中隐藏。
    *   **课时记录**: 完整的签到（-1）和充值（+X）账本，并显示实时余额。
    *   **增加课时 (Add Classes)**: 轻松为学生账户增加课时（例如充值或调整）。
*   **每日报告 (Daily Reports)**:
    *   查看所选日期的按时间顺序排列的签到列表。
    *   **计数徽章**: 标题栏显示当天的总签到人数。
    *   显示学生姓名、联系邮箱和剩余课时余额。
*   **批量导入 (Bulk Import)**: 使用简单的文本格式快速添加多名学生。智能处理重名情况，提示确认或重命名。并在顶部显示当前的活跃学生总数 (Active Count)。
*   **导出数据**:
    *   **备份**: 下载包含所有应用数据的完整 JSON 备份。
    *   **Excel 导出**: 将学生名单（包含活跃状态和余额）及交易历史记录导出为 Excel 文件 (.xlsx)。

### ⚙️ 配置与数据
*   **基于课时的系统**: 使用“课时数”（整数）而非货币金额来追踪出勤。
*   **本地优先**: 使用 Dexie.js (IndexedDB) 支持离线工作。
*   **手动备份/恢复**: 随时下载数据的 JSON 备份或从文件恢复。

## 🛠️ 技术栈

*   **HTML5**
*   **Tailwind CSS** (通过 CDN 获取样式)
*   **Alpine.js** (通过 CDN 获取响应式和状态管理)
*   **Dexie.js** (IndexedDB 的封装库)
*   **SheetJS** (用于 Excel 导出)

## 🚀 安装与设置

由于这是一个静态 Web 应用程序，不需要后端服务器。

1.  **克隆仓库:**
    ```bash
    git clone https://github.com/skyartny/skyartny.github.io.git
    cd skyartny.github.io
    ```

2.  **本地运行:**
    您可以使用任何静态文件服务器。例如，使用 Python:
    ```bash
    python3 -m http.server 8000
    ```
    然后在浏览器中打开 `http://localhost:8000`。

## 📖 使用指南

### 1. 初始设置
*   首次访问 **ROSTER** (花名册) 标签页时，系统会要求您 **Setup Admin** (设置管理员)。
*   创建一个安全密码并回答 **Security Question** (安全问题，用于离线恢复)。

### 2. 添加学生
*   进入 **ROSTER** (输入管理员密码)。
*   点击 **+ BULK IMPORT**。
*   输入姓名（例如 `John Doe` 或 `Doe, John`），每行一个。
*   点击 **IMPORT LIST**。
*   如果姓名已存在，系统会提示您重命名（例如改为 "John Doe 2"）或跳过。

### 3. 管理课时
*   **增加课时**: 在 Roster 中点击学生姓名。在 **Add Classes** 下方，输入课时数（例如 10）并点击 **ADD**。
*   **编辑姓名**: 在详情视图中点击学生姓名即可进行重命名。

### 4. 每日签到
*   进入 **CHECK-IN** 标签页。
*   输入学生姓名。
*   点击黑色的 **CHECK IN** 按钮。
*   系统将扣除 **1 课时**。
*   该学生将移至列表底部并显示为 "Checked In"。

## 🔒 安全性

*   **管理员密码**: 以 SHA-256 哈希形式存储在本地数据库中。**绝不会**以明文形式发送到服务器。
*   **数据隐私**: 所有数据均驻留在您的浏览器本地存储中。未使用外部数据库。

### ⚠️ 忘记密码如何重置 (清除 IndexedDB)

如果您忘记了管理员密码且无法通过安全问题找回，您可以 **重置应用程序**。这将删除所有本地数据，因此请仅在有备份或愿意重新开始的情况下执行此操作。

#### 方法 1: 使用 Chrome 开发者工具 (推荐)
1.  在浏览器中打开应用程序。
2.  按 **F12** (或 Mac 上的 `Cmd+Option+I`) 打开开发者工具。
3.  进入 **Application** (应用) 标签页。
4.  在左侧边栏的 **Storage** 下：
    *   点击 **IndexedDB** -> **Delete database**。
    *   或者点击 **Storage** -> **Clear site data**。
5.  刷新页面。系统将提示您设置新的管理员账户。

#### 方法 2: 通过代码 (控制台)
1.  打开浏览器控制台 (`F12` -> Console)。
2.  运行以下命令:
    ```javascript
    indexedDB.deleteDatabase("FlexCheck_V4").onsuccess = function() {
        localStorage.clear();
        location.reload();
    };
    ```
3.  页面将刷新，您可以重新开始。

## 📄 许可证

本项目开源并基于 MIT 许可证发布。
